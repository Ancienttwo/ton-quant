{
  "name": "tonquant-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --filter tonquant dev",
    "build": "bun run --filter tonquant build",
    "typecheck": "tsc -b packages/core/tsconfig.json apps/cli/tsconfig.json",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "bun test",
    "test:coverage": "bun test --coverage"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@types/bun": "latest",
    "bun-types": "^1.3.11",
    "typescript": "latest"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
